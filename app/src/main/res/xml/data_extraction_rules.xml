<?xml version="1.0" encoding="utf-8"?>
<!-- Same idea as backup_rules: the user owns their data through export files,
     so device transfer and cloud backup are left out on purpose. -->
<data-extraction-rules>
    <cloud-backup>
    </cloud-backup>
    <device-transfer>
    </device-transfer>
</data-extraction-rules>
